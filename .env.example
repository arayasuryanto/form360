# Formure no longer reads from .env at runtime — config is in config.js.
# This file is kept only as a reference for what to put into config.js.

# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_KEY=your-anon-key
